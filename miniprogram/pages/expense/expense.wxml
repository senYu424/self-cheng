<view class="expense-container">
  <!-- 支出/收入切换 -->
  <view class="type-tabs">
    <view class="tab-item {{type === 'expense' ? 'active expense' : ''}}" bindtap="onTypeChange" data-type="expense">
      支出
    </view>
    <view class="tab-item {{type === 'income' ? 'active income' : ''}}" bindtap="onTypeChange" data-type="income">
      收入
    </view>
  </view>

  <view class="amount-section {{type}}">
    <text class="label">金额</text>
    <view class="amount-input">
      <text class="currency">{{type === 'expense' ? '-' : '+'}}¥</text>
      <input type="digit" value="{{amount}}" placeholder="0.00" bindinput="onAmountInput" />
    </view>
  </view>

  <view class="form-section">
    <view class="form-item">
      <text class="label">分类</text>
      <view class="category-list">
        <view wx:for="{{type === 'expense' ? expenseCategories : incomeCategories}}" wx:key="*this"
              class="category-item {{category === item ? 'active ' + type : ''}}"
              bindtap="onCategorySelect" data-category="{{item}}">
          {{item}}
        </view>
      </view>
    </view>

    <view class="form-item">
      <text class="label">日期</text>
      <picker mode="date" value="{{date}}" bindchange="onDateChange">
        <view class="picker">{{date}}</view>
      </picker>
    </view>

    <view class="form-item">
      <text class="label">备注</text>
      <input placeholder="添加备注..." value="{{note}}" bindinput="onNoteInput" />
    </view>

    <view class="form-item" wx:if="{{type === 'expense'}}">
      <text class="label">支付方式</text>
      <radio-group bindchange="onPaymentChange">
        <label><radio value="wechat" checked="{{paymentMethod === 'wechat'}}" />微信</label>
        <label><radio value="alipay" checked="{{paymentMethod === 'alipay'}}" />支付宝</label>
        <label><radio value="cash" checked="{{paymentMethod === 'cash'}}" />现金</label>
        <label><radio value="other" checked="{{paymentMethod === 'other'}}" />其他</label>
      </radio-group>
    </view>

    <view class="form-item" wx:if="{{type === 'expense'}}">
      <text class="label">家庭共享</text>
      <switch checked="{{isShared}}" bindchange="onSharedChange" />
    </view>
  </view>

  <button class="submit-btn {{type}}" bindtap="submitExpense">保存</button>
</view>
